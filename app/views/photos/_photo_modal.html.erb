<!-- Modal Header -->
<div class="modal-header">
    <h4><b><%= photo.title %></b></h4>
    <button type="button" class="close" data-dismiss="modal">×</button>
  </div>
<!-- Modal body -->
  <div class="modal-body">
    <img class="rounded-sm w-100 h-100" src="<%= url_for(photo.image) %>">
  </div>
<!-- Modal footer -->
  <div class="modal-header">
    <p><%= photo.description %></p>
  </div>